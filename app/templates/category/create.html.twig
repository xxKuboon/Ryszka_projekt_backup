{% extends 'base.html.twig' %}

{% block title %}
    {{ 'Tworzenie nowej kategorii'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'Tworzenie nowej kategorii'|trans }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'Dodaj nową kateogrię'|trans }}" class="btn btn-primary"/>
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('category_index') }}" class="btn btn-link">
            {{ 'Powrót do listy'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}
